FROM node:22-alpine

WORKDIR /app

# Install dependencies in a cached layer. Source code is bind-mounted at runtime.
COPY package*.json ./
RUN npm ci

ENV NODE_ENV=development

EXPOSE 5000

CMD ["npm", "run", "dev"]
